<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="UCMSApp.VVM.MainMenu.AdminMenu"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:vm="clr-namespace:UCMSApp.VVM.MainMenu"
    Title="{Binding Title}"
    x:DataType="vm:AdminMenuViewModel">
    <VerticalStackLayout Spacing="10" VerticalOptions="CenterAndExpand">
        <Button
            Command="{Binding GoToCompanyViewCommand}"
            FontAttributes="Bold"
            Text="Компания"
            WidthRequest="200" />
        <Button
            Command="{Binding GoToSamplesViewCommand}"
            FontAttributes="Bold"
            Text="Шаблоны"
            WidthRequest="200" />
        <Button
            Command="{Binding GoToUserMenuViewCommand}"
            FontAttributes="Bold"
            Text="Меню сотрудника"
            WidthRequest="200" />
    </VerticalStackLayout>
</ContentPage>